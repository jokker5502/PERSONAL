'use client'

/*
Sección pendiente por fallos al usar el SDK de Firebase en producción
*/

import LoginPage from '@/modules/home/login/page'
const ParentComponent = () => {
	return (
		<div>
			<LoginPage />
		</div>
	)
}

export default ParentComponent
